# Cobalt

New split structure for the Cobalt IT Services website prototype.

## Structure

- `frontend/` Vite + React app
- `backend/` Express + Node.js API

## Next steps

1. Install dependencies in each folder.
2. Wire the frontend to the backend API endpoints.
3. Replace placeholder visual cards with final brand imagery and CMS content.
# Cobalt_IT_Services
